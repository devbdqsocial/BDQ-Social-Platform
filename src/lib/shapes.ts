// Brand-card mask cycle (shape-language §repetition). Near-square forms only, so cards/carousel
// items keep even heights while the mask varies by index. Consumed by VendorDiscover + BrandsCarousel.
export const CARD_SHAPES = ["svg--form2", "svg--form10", "svg--form6", "svg--form1"] as const;
