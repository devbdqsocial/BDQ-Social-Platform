/**
 * Brand color constants for surfaces that cannot read CSS variables (Razorpay checkout modal,
 * OG/app icons, HTML email, konva canvas defaults). Values mirror design-system.md §1.1 —
 * change THERE first, then here. Everything else uses the tokens in globals.css.
 */
export const BRAND_NAVY = "#01065B";
export const BRAND_LAVENDER = "#868EFF";
export const BRAND_CREAM = "#F4F2EC";
export const BRAND_INK = "#14141A";
